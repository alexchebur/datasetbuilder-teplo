# ⚖️ Сборщик датасета судебных актов (HTML/JS)

Веб-приложение для сбора и подготовки датасета судебных решений арбитражных судов. Работает полностью в браузере — данные не отправляются на сервер.

## 🚀 Развёртывание на GitHub Pages

### Шаг 1: Создайте репозиторий
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/your-username/court-dataset-builder.git
git push -u origin main
